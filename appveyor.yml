environment:
  matrix:
    - nodejs_version: "g"

  install:
    - ps: Install-Product node $env:nodejs_version
    - npm Install

  test_script:
    - node --version
    - npm --version
    - npm test

  build: off    
